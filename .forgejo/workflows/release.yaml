on:
  push:
    tags:
      - v[0-9]+.[0-9]+.*

jobs:
  release:
    runs-on: codeberg-tiny
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
